pkgname=ttf-league-raleway
pkgver=20210727
pkgrel=1
pkgdesc="The League of Moveable Type family: raleway"
arch=('any')
url='https://github.com/theleagueof/raleway'
license=('OFL')
depends=('fontconfig')
provides=('ttf-font')
source=("https://github.com/theleagueof/raleway/archive/aaa7d07d673ff276bcecb055dc2cf36e639e4a54.tar.gz")
sha256sums=("07022f08b2dbe38ec7e4eb27381ccdc198fa1d7abd82bd40522d9a68d38b4586")

package() {
  install -dm755 "${pkgdir}/usr/share/fonts/TTF/${pkgname}"

  # Install all shipped OpenType/TrueType font binaries from the repository archive.
  find "${srcdir}" -type f \( -name '*.ttf' -o -name '*.otf' -o -name '*.ttc' \) -print0 | while IFS= read -r -d '' font; do
    install -m644 "${font}" "${pkgdir}/usr/share/fonts/TTF/${pkgname}/"
  done

  license_file="$(find "${srcdir}" -type f \( -iname 'OFL*' -o -iname 'LICENSE*' \) | head -n 1)"
  if [[ -n "${license_file}" ]]; then
    install -Dm644 "${license_file}" "${pkgdir}/usr/share/licenses/${pkgname}/LICENSE"
  fi
}
